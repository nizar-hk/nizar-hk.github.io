source "https://rubygems.org"

# Use the GitHub Pages gem so the build environment matches GitHub Pages
gem "github-pages", group: :jekyll_plugins

# If you use a remote theme, uncomment/add:
# gem "jekyll-remote-theme"

# Add any other Jekyll plugins you actually use here, for example:
# gem "jekyll-seo-tag"
# gem "jekyll-feed"
